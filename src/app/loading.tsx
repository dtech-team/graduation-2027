import { FullScreenLoader } from "@/components/FullScreenLoader";

export default function Loading() {
  return <FullScreenLoader text="ĐANG TẢI DỮ LIỆU..." subText="Đợi xíu, mạng đang đi bộ..." />;
}
